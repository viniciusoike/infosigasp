# cache clearing validates its controls

    Code
      clear_infosiga_cache(processed = NA)
    Condition
      Error in `clear_infosiga_cache()`:
      ! `processed`, `source` and `quiet` must be `TRUE` or `FALSE`.

