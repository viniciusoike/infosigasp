% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{infosiga_download}
\alias{infosiga_download}
\title{Download the INFOSIGA-SP source archive}
\usage{
infosiga_download(overwrite = FALSE, quiet = FALSE, timeout = 3600)
}
\arguments{
\item{overwrite}{Logical. If \code{FALSE} (default) and the archive is already
cached, the existing file is kept and returned. Set to \code{TRUE} to download
again and replace it.}

\item{quiet}{Logical. If \code{FALSE} (default), report progress with
informative messages.}

\item{timeout}{Download timeout in seconds. The archive is large (around
120 MB), so the default temporarily raises \code{\link[=options]{options()}}\verb{$timeout} to
\code{3600}. Pass a larger value on slow connections.}
}
\value{
The path to the cached archive, invisibly.
}
\description{
Downloads the consolidated INFOSIGA-SP data archive (\code{dados_infosiga.zip})
from DETRAN-SP into the local cache. Most users do not need to call this
directly: \code{\link[=read_infosiga]{read_infosiga()}} downloads the archive on demand. Use this
function when you want to pre-fetch the data (for example, before going
offline) or to force a refresh.
}
\details{
The archive is updated monthly by DETRAN-SP and accumulates all records
from 2015 onward. The download URL can be overridden with the
\code{infosigasp.zip_url} option, which may be a character vector of mirror URLs
tried in order until one succeeds. The default is the official DETRAN-SP
endpoint followed by a GitHub-release mirror that serves a point-in-time
snapshot when the official portal is unavailable. Override the option to add
your own mirror or for testing.

Because DETRAN-SP overwrites the archive in place each month under the same
file name, a cached copy can become stale silently. When a cached archive is
reused that is older than the \code{infosigasp.stale_days} option (30 days by
default; set to \code{Inf} to disable), a warning suggests refreshing it. The age
is taken from the cached file's modification time.
}
\examples{
\dontrun{
# Pre-fetch the archive into the cache
infosiga_download()

# Force a refresh after a monthly update
infosiga_download(overwrite = TRUE)
}
}
\seealso{
\code{\link[=read_infosiga]{read_infosiga()}} to import the data, and \code{\link[=infosiga_cache_dir]{infosiga_cache_dir()}}
to locate the cache.
}
