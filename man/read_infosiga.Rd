% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{read_infosiga}
\alias{read_infosiga}
\title{Import an INFOSIGA-SP dataset}
\usage{
read_infosiga(
  dataset = c("sinistros", "pessoas", "veiculos"),
  year = NULL,
  download_if_missing = TRUE,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{dataset}{Which dataset to import. One of:
\describe{
\item{\code{"sinistros"}}{Crash events (one row per event).}
\item{\code{"pessoas"}}{Victims / people involved (one row per person).}
\item{\code{"veiculos"}}{Vehicles involved (one row per vehicle).}
}}

\item{year}{Optional integer vector used to filter rows by year of the
crash (\code{ano_sinistro}). If \code{NULL} (default), all available years are
returned. For example, \code{year = 2020:2023}.}

\item{download_if_missing}{Logical. If \code{TRUE} (default), download the
archive when it is not already cached. If \code{FALSE} and the archive is
missing, an informative error is raised.}

\item{quiet}{Logical. If \code{FALSE} (default), report progress.}

\item{...}{Additional arguments passed to \code{\link[=infosiga_download]{infosiga_download()}} (for
example \code{overwrite = TRUE} to force a refresh).}
}
\value{
A \link[tibble:tibble]{tibble} with one row per record. The columns
keep the original INFOSIGA-SP names (in Portuguese); see the package data
dictionary via \code{\link[=infosiga_dictionary]{infosiga_dictionary()}}. The three datasets can be joined
on \code{id_sinistro} (and \code{id_veiculo}, where present).
}
\description{
Downloads (if necessary) and imports one of the three INFOSIGA-SP datasets
as a tidy tibble. The source archive is cached locally, so the first call
triggers a download and subsequent calls read from disk.
}
\details{
Source files are encoded in Latin-1 (ISO-8859-1), use \verb{;} as the field
separator, \verb{,} as the decimal mark and \code{DD/MM/YYYY} dates. \code{read_infosiga()}
handles all of these and returns UTF-8 text, \code{Date} columns and numeric
coordinates. Each dataset is distributed across two period files inside the
archive (2015-2021 and 2022 onward); they are read and row-bound
transparently.

The imported data follow the source as closely as possible and are not
otherwise cleaned. A small fraction of rows in the source contain
data-quality issues (for example, an unescaped \verb{;} inside a street name, or
mis-encoded \code{latitude}/\code{longitude} values); coordinates in particular should
be sanity-checked before mapping. Any value that cannot be parsed to its
declared column type is set to \code{NA} and recorded by \code{\link[readr:problems]{readr::problems()}}.

Empty fields are read as \code{NA}. Note that many categorical columns instead
use the literal value \code{"NAO DISPONIVEL"} ("not available") to flag missing
information; these are preserved as-is so the imported data matches the
source. The crash-type flag columns (\verb{tp_sinistro_*}) hold \code{"S"} when the
flag applies and \code{NA} otherwise.
}
\examples{
\dontrun{
# Import all crash events (downloads the archive on first use)
sinistros <- read_infosiga("sinistros")

# Only victims from 2022 and 2023
vitimas <- read_infosiga("pessoas", year = 2022:2023)
}

# A bundled sample (no download required) illustrates the structure:
sample_path <- system.file(
  "extdata", "sinistros_sample.csv",
  package = "infosigasp"
)
if (nzchar(sample_path)) head(readr::read_delim(sample_path, ";"))

}
\seealso{
\code{\link[=infosiga_download]{infosiga_download()}}, \code{\link[=infosiga_cache_dir]{infosiga_cache_dir()}},
\code{\link[=infosiga_dictionary]{infosiga_dictionary()}}.
}
