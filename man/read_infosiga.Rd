% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{read_infosiga}
\alias{read_infosiga}
\title{Import an INFOSIGA-SP dataset}
\usage{
read_infosiga(
  dataset = c("sinistros", "pessoas", "veiculos"),
  processing = c("clean", "typed", "raw"),
  standardize = NULL,
  refresh = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{dataset}{Which dataset to import.
\describe{
\item{\code{"sinistros"}}{Occurrence records: confirmed crashes and
notifications (one row per record).}
\item{\code{"pessoas"}}{Victims / people involved (one row per person).}
\item{\code{"veiculos"}}{Vehicles involved (one row per vehicle).}
}}

\item{processing}{Level of processing to apply. \code{"raw"} imports every field
as character and preserves empty strings, whitespace, sentinels and source
representations. \code{"typed"} parses the documented column classes without
applying additional cleaning. \code{"clean"} (default) adds the package's
cleaning pipeline to the typed import.}

\item{standardize}{Optional character vector selecting label harmonisation:
\code{"municipios"} restores official municipality spellings and harmonises
administrative-region names; \code{"cores"} merges duplicate vehicle-colour
spellings; and \code{"profissoes"} applies consistent title case and missing-value
markers to occupations. Use \code{"all"} for every option applicable to the
selected dataset. Requires \code{processing = "clean"}.}

\item{refresh}{Logical. If \code{TRUE}, download the latest available source data
before reading. If \code{FALSE} (default), reuse the copy in the local
\code{infosigasp} cache, downloading it only when it is missing.}

\item{quiet}{Logical. If \code{FALSE} (default), report progress.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per record. The columns
keep the original INFOSIGA-SP names (in Portuguese); see the package data
dictionary via \code{\link[=dictionary_infosiga]{dictionary_infosiga()}}. The three datasets can be joined
on \code{id_sinistro} (and \code{id_veiculo}, where present).
}
\description{
Downloads (if necessary) and imports one of the three INFOSIGA-SP datasets
as a tidy tibble. The first interactive call asks before downloading about
120 MB to the user's local cache; subsequent calls read from disk.
}
\details{
Source files are encoded in Latin-1 (ISO-8859-1), use \verb{;} as the field
separator, \verb{,} as the decimal mark and \code{DD/MM/YYYY} dates. Every mode
decodes text to UTF-8, parses the CSV structure and row-binds the period files.
The modes differ in what happens to the fields after that tabular import.
\itemize{
\item \code{processing = "raw"} returns a lossless tabular representation: every
field is character, including dates and numbers, while empty strings,
whitespace, sentinels and malformed representations remain visible. This
is not a byte-for-byte copy because encoding is decoded and period files
are combined.
\item \code{processing = "typed"} parses the documented column classes. Dates become
\code{Date}, times become \code{hms}, numeric fields become integer or double,
empty fields become \code{NA}, and identifiers remain character. Category
labels, padding and explicit source sentinels otherwise remain unchanged.
\item \code{processing = "clean"} starts from the typed import, then trims text, maps
\code{"NAO DISPONIVEL"} to \code{NA}, orders ordinal columns, parses \verb{ano_mes_*},
converts crash-type flags to logical, converts \code{tempo_sinistro_obito} to
integer, removes a trailing \code{".0"} from \code{numero_logradouro}, and validates
coordinate pairs against the Sao Paulo state boundary with a 2 km buffer.
Missing \verb{qtd_*} counts remain \code{NA}.
}

Before converting a closed-domain column, the cleaning step validates its
observed values. If an ordinal column, crash-type flag or integer field
contains an unexpected representation, the entire source column is preserved
and a warning identifies the new values. This prevents upstream changes from
becoming missing values or incorrect \code{FALSE} values silently.

Label harmonisation is selective and opt-in. \code{standardize = "municipios"}
uses official IBGE municipality names keyed by \code{cod_ibge} and harmonises the
spelling of INFOSIGA administrative regions. \code{"cores"} merges case and gender
variants of basic vehicle colours, while preserving detailed liveries and
multi-tone values. \code{"profissoes"} applies consistent title case and known
missing-value markers to occupation labels. These transformations preserve
the rows and columns and never create broader analytical categories.

A small fraction of rows in the source contain data-quality issues (for
example, an unescaped \verb{;} inside a street name, or mis-encoded coordinates).
In typed and clean modes, values that cannot be parsed to their declared
column type become \code{NA} and are recorded by \code{\link[readr:problems]{readr::problems()}}. Raw mode
preserves those field values as character. Structural CSV problems are
recorded in every mode.
}
\section{Coverage and known caveats}{


The source data are published as a single continuous series from 2015
onward, but the \emph{scope} of what is collected has changed over time and
several columns carry definitions that are easy to misread. None of these
are import errors, so the package does not alter the data; they do,
however, invalidate a number of otherwise reasonable analyses.

\describe{
\item{\strong{2015--2018 covers fatal crashes only.}}{This is the single most
consequential caveat. Non-fatal records begin in 2019: for 2015--2018
\code{tipo_registro} is \code{"SINISTRO FATAL"} for every row, and in \code{pessoas}
the \code{"LEVE"} and \code{"GRAVE"} injury levels do not occur at all. Counting
crashes or victims per year across the whole series therefore shows an
apparent 20-fold jump in 2019 that is entirely an artefact of the
expanded collection scope. \strong{For any trend that includes years before
2019, restrict to fatalities} (\code{tipo_registro == "SINISTRO FATAL"}, or
\code{gravidade_lesao == "FATAL"} in \code{pessoas}); otherwise start the series
in 2019.}
\item{\strong{\code{tipo_registro} mixes notifications with confirmed crashes.}}{
About a third of \code{sinistros} rows are \code{"NOTIFICACAO"}, a reported event
that has not been confirmed as a crash. Treating every row as a crash
overstates the total substantially. Filter on \code{tipo_registro} according
to whether you want confirmed events only.}
\item{\strong{The most recent months are provisional.}}{DETRAN-SP reclassifies
records as it validates them, so the newest months carry an unusually
high share of \code{"NOTIFICACAO"} and an artificially low share of confirmed
crashes. The final month or two of any release is also partial. Recent
periods are not comparable with settled ones; drop the tail of the series
before computing trends.}
\item{\strong{\code{tempo_sinistro_obito} is capped at 30 days.}}{The published
values never exceed 30, matching the 30-day convention for attributing a
death to a crash. Deaths occurring later are not recorded as
crash-related here, so fatality counts are 30-day counts, not lifetime
ones.}
\item{\strong{The \verb{qtd_*} vehicle counts do not always match \code{veiculos}.}}{
Summing the \code{qtd_pedestre} .. \code{qtd_veic_nao_disponivel} columns
disagrees with the number of matching \code{veiculos} rows for a small share
of crashes, so the two routes to "how many vehicles" give different
answers; pick one and state it. The \verb{qtd_gravidade_*} columns, by
contrast, agree with the \code{pessoas} row counts for every crash.}
\item{\strong{Coordinate availability varies sharply by year.}}{After the
bounding-box validation described above, nearly every
crash in the middle years of the series has usable coordinates, against a
materially smaller share in the earliest and the most recent years.
Mapped subsets are therefore not a uniform sample over time.}
\item{\strong{Not every crash has victim or vehicle rows.}}{Around a third of
\code{sinistros} records have no matching row in \code{pessoas}. Use a left join
when you need to keep all crashes, and expect \code{NA} on the victim side.}
}

The figures above describe the 2026 releases and shift slightly as
DETRAN-SP revises the data; the structural points do not.
}

\examples{
\dontrun{
# Import all occurrence records, cleaned (downloads on first use)
sinistros <- read_infosiga("sinistros")
levels(sinistros$dia_da_semana)

# Import all victims / people involved
vitimas <- read_infosiga("pessoas")

# Lossless tabular import: every field is character
raw <- read_infosiga("sinistros", processing = "raw")

# Parse documented classes without further cleaning
typed <- read_infosiga("sinistros", processing = "typed")
}

# A bundled sample (no download required) illustrates the structure:
sample_path <- system.file(
  "extdata", "sinistros_sample.csv",
  package = "infosigasp"
)
if (nzchar(sample_path)) head(readr::read_delim(sample_path, ";"))

}
\seealso{
\code{\link[=dictionary_infosiga]{dictionary_infosiga()}}.
}
