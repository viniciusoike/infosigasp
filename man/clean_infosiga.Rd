% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean.R
\name{clean_infosiga}
\alias{clean_infosiga}
\title{Clean and process an INFOSIGA-SP dataset}
\usage{
clean_infosiga(data, dataset = c("sinistros", "pessoas", "veiculos"))
}
\arguments{
\item{data}{A data frame imported with \code{\link[=read_infosiga]{read_infosiga()}} (typically with
\code{clean = FALSE}).}

\item{dataset}{Which dataset \code{data} corresponds to: \code{"sinistros"},
\code{"pessoas"} or \code{"veiculos"}. Determines which columns are processed.}
}
\value{
A \link[tibble:tibble]{tibble} with the same columns as \code{data}, with the
processing described in \emph{Details} applied.
}
\description{
Applies the standard processing that \code{\link[=read_infosiga]{read_infosiga()}} performs by default
(\code{clean = TRUE}). Use this directly only when you imported a dataset with
\code{clean = FALSE} (the raw version) and want to process it afterwards.
}
\details{
The processing standardises missing values, fixes source formatting
artefacts and assigns meaningful types to columns whose published
representation is inconvenient (ordinal text, binary flags, year-month
strings). It never renames columns, recodes category labels or drops rows.

\code{clean_infosiga()} applies the following steps, in order. Every step is
idempotent, so calling the function again on an already-processed dataset
changes nothing.

\enumerate{
\item \strong{Whitespace.} Trims leading and trailing whitespace from every text
column. Some source fields are space-padded to a fixed width (for example
\code{nacionalidade} is published as \code{"BRASILEIRA          "}); without
trimming, comparisons, grouping and joins on those columns silently fail.
\item \strong{Missing values.} Replaces the literal \code{"NAO DISPONIVEL"} ("not
available") marker with \code{NA} in every text column. Trimming runs first,
so space-padded markers are also caught.
\item \strong{Ordered factors.} Ordinal columns become \strong{ordered factors} in
their natural order.
\itemize{
\item \code{dia_da_semana}: \code{Domingo} < ... < \code{Sabado} (the Brazilian week
starts on Sunday).
\item \code{turno}: \code{MADRUGADA} < \code{MANHA} < \code{TARDE} < \code{NOITE}.
\item \code{gravidade_lesao} (in \code{pessoas}): \code{LEVE} < \code{GRAVE} < \code{FATAL}.
\item \code{faixa_etaria_demografica}, \code{faixa_etaria_legal} (in \code{pessoas}):
age bands in increasing order.
}
\item \strong{Year-month dates.} Parses the year-month columns
(\code{ano_mes_sinistro}, \code{ano_mes_obito}), published as \code{"YYYY/MM"} strings,
to first-of-month \code{Date} values, matching the \code{Date} class already used
for the full-date columns.
\item \strong{Crash-type flags} (\code{sinistros}). The binary \verb{tp_sinistro_*}
columns become \strong{logical} (\code{TRUE} / \code{FALSE}). They mark whether a crash
involved a given event type and are published as \code{"S"} (yes) or empty
(no). The categorical \code{tp_sinistro_primario} (the primary crash type,
e.g. \code{"COLISAO"}) is \emph{not} a flag and stays text.
\code{tp_sinistro_colisao_traseira} is empty for every record in the source
and therefore becomes uniformly \code{FALSE}. That is an unpopulated upstream
field, not evidence that no rear-end collisions occurred. A crash may
also set several flags at once, so the flags do not partition the data.
\item \strong{Count columns} (\code{sinistros}). The \verb{qtd_*} columns form two
independent blocks: vehicle counts (\code{qtd_pedestre}, \code{qtd_automovel},
...) and victim-severity counts (\verb{qtd_gravidade_*}). A blank entry inside
a block that is otherwise filled in means \emph{zero} and becomes \code{0L}. When a
record carries no breakdown at all, the whole block is blank; those
blanks are genuinely "not recorded" and stay \code{NA}. The two blocks are
handled separately because many records carry one but not the other.
\item \strong{Days to death} (\code{pessoas}). \code{tempo_sinistro_obito}, the number of
days between the crash and the victim's death (published as a numeric
string), becomes \strong{integer}.
\item \strong{Street numbers and kilometre markers} (\code{sinistros}).
\code{numero_logradouro} carries a house number on urban streets but a
\strong{kilometre marker} on highways, where a fractional part is meaningful
(\code{"0.25"} is km 250 m, not a malformed house number). Genuine decimals
are common on \verb{ESTRADAS E RODOVIAS} rows and rare on \verb{VIAS URBANAS} ones.
Only a trailing \code{".0"} from the source export is stripped (\code{"193.0"} ->
\code{"193"}); any other decimal part survives. The column stays character
because the two meanings are not comparable on a single numeric scale.
\item \strong{Coordinates} (\code{sinistros}). Validates \code{latitude}/\code{longitude} as a
pair against the bounding box of the state of Sao Paulo. Points outside
the box, which are mis-encoded values and \verb{(0, 0)} "null island"
placeholders, have both coordinates set to \code{NA}. This affects a few
percent of records and drops no rows. Use \code{clean = FALSE} if you need the
raw coordinates.
}

Nominal text columns (such as \code{municipio}, \code{tipo_via} or \code{sexo}) stay
character vectors. Well-typed numeric columns, notably \code{idade} (the victim's
age, in \code{pessoas}), pass through unchanged and are \emph{not} range-checked.
Missing ages are \code{NA} and ages of \code{0} (infants) are kept. The package
enforces no bound on \code{idade}, so validate it yourself if your analysis is
sensitive to outliers.
}
\examples{
# Process the bundled raw sample
raw <- readr::read_delim(
  system.file("extdata", "pessoas_sample.csv", package = "infosigasp"),
  delim = ";", show_col_types = FALSE
)
clean <- clean_infosiga(raw, "pessoas")
levels(clean$gravidade_lesao)
}
\seealso{
\code{\link[=read_infosiga]{read_infosiga()}}, which calls this function when \code{clean = TRUE}.
}
