% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{infosiga_cache}
\alias{infosiga_cache}
\alias{infosiga_cache_dir}
\alias{infosiga_cache_list}
\alias{infosiga_cache_clear}
\title{Manage the infosigasp on-disk cache}
\usage{
infosiga_cache_dir()

infosiga_cache_list()

infosiga_cache_clear(confirm = interactive())
}
\arguments{
\item{confirm}{Logical. If \code{TRUE} (the default in interactive sessions),
ask for confirmation before deleting cached files. Set to \code{FALSE} to
delete without prompting (e.g. in scripts).}
}
\value{
\itemize{
\item \code{infosiga_cache_dir()} returns the cache directory path (a string),
creating it if necessary.
\item \code{infosiga_cache_list()} returns a character vector of cached file paths
(possibly empty).
\item \code{infosiga_cache_clear()} invisibly returns the paths it removed.
}
}
\description{
INFOSIGA-SP ships its data as a single archive of roughly 120 MB
(uncompressed, over 700 MB). To avoid repeated downloads, infosigasp stores
the archive in a per-user cache directory and reuses it across sessions.
These functions inspect and manage that cache.
}
\details{
The cache location defaults to the operating-system specific user cache
directory returned by \code{\link[tools:userdir]{tools::R_user_dir()}} (\code{"infosigasp"}, \code{"cache"}).
You can override it for the current session with the \code{infosigasp.cache_dir}
option, e.g. \code{options(infosigasp.cache_dir = "~/my-cache")}, or permanently
through your \code{.Rprofile}.
}
\examples{
# Where does infosigasp cache its files?
infosiga_cache_dir()

# What is currently cached?
infosiga_cache_list()

}
