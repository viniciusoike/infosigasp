% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/labels.R
\name{tidy_infosiga_labels}
\alias{tidy_infosiga_labels}
\title{Tidy the category labels of an INFOSIGA-SP dataset}
\usage{
tidy_infosiga_labels(data, dataset = c("sinistros", "pessoas", "veiculos"))
}
\arguments{
\item{data}{A data frame from \code{\link[=read_infosiga]{read_infosiga()}} or \code{\link[=clean_infosiga]{clean_infosiga()}}.}

\item{dataset}{Which dataset \code{data} corresponds to: \code{"sinistros"},
\code{"pessoas"} or \code{"veiculos"}. Determines which columns are rewritten.}
}
\value{
A \link[tibble:tibble]{tibble} with the same rows as \code{data}. Columns are
unchanged except as described in \emph{Details}; \code{sinistros} additionally gains
a \code{conservacao_codigo} column.
}
\description{
An \strong{opt-in} companion to \code{\link[=clean_infosiga]{clean_infosiga()}} that rewrites category labels
into a consistent, presentation-ready form: Title Case, accents restored
where an authoritative spelling exists, duplicate categories merged, and the
source's assorted "not informed" strings mapped to \code{NA}.
}
\details{
It stays separate from \code{\link[=clean_infosiga]{clean_infosiga()}}, which never touches labels. Use
\code{clean_infosiga()} (or \code{read_infosiga(clean = TRUE)}) when you need
categories exactly as DETRAN-SP publishes them, for example to reproduce an
official figure. Use \code{tidy_infosiga_labels()} on top of that when you are
doing your own analysis and want labels that group, sort and plot sensibly.

\subsection{Place names (sinistros, pessoas)}{
The source publishes \code{municipio} unaccented and upper case (\code{"SAO PAULO"})
but \code{regiao_administrativa} with its accents intact, so neither can be
joined to other Brazilian data as published. Both take the official IBGE
spelling, matched on \code{cod_ibge} and never on the name. Nine municipalities
differ between the two sources: eight apostrophes that INFOSIGA renders as
spaces (\code{"SANTA BARBARA D OESTE"}), plus one genuine spelling difference,
IBGE's authoritative \code{"Sao Luiz do Paraitinga"} against INFOSIGA's
\code{"SAO LUIS DO PARAITINGA"}. See \link{infosiga_municipios}.
}

\subsection{Vehicle colour (veiculos)}{
\code{cor_veiculo} carries dozens of distinct values for roughly sixteen real
colours, because two upstream systems coexist in every year: an upper-case
stream (\code{"PRETA"}, \code{"BRANCA"}) and a title-case one with different gender
agreement (\code{"Preta"}, \code{"Branco"}). Note that \code{toupper()} alone will \emph{not}
merge these. Values map onto a canonical set. Single-colour official liveries
(\code{"BRANCA (PADRAO PM)"}) fold into the base colour, two- and three-tone
values (\code{"CIN/VER/PRE"}) become \code{"Multicor"}, and camouflage becomes
\code{"Camuflada"}. Unrecognised values pass through unchanged, so a colour the
mapping does not know is never dropped.
}

\subsection{Occupation (pessoas)}{
\code{profissao} is Title Cased, which merges the several hundred values that
differ from another value only by capitalisation, and \code{"NAO INFORMADA"} /
\code{"Nao informada"} become \code{NA}. Accents are \strong{not} restored here and
occupations are \strong{not} grouped semantically, because no authoritative
spelling list covers these free-text values. Expect near-duplicates to
remain.
}

\subsection{Road maintenance (sinistros)}{
\code{conservacao} mixes two vocabularies: the name of the body that maintains
the road (\code{"PREFEITURA"}, \code{"NOVADUTRA"}) and bare route codes (\code{"10.03"}),
the latter covering tens of thousands of rows. The codes move into a new
\code{conservacao_codigo} column, so \code{conservacao} holds names only.

Those names keep their source capitalisation, unlike every other column here.
Most are brands or acronyms (\code{"SPMAR"}, \code{"TEBE"}, \code{"CART"}, \code{"AUTOBAN"},
\code{"DNIT"}), and Title Case would corrupt them into \code{"Spmar"} and \code{"Tebe"}.
}

Every step is idempotent: calling \code{tidy_infosiga_labels()} on an
already-tidied dataset changes nothing.
}
\examples{
raw <- readr::read_delim(
  system.file("extdata", "veiculos_sample.csv", package = "infosigasp"),
  delim = ";", show_col_types = FALSE
)
tidied <- tidy_infosiga_labels(clean_infosiga(raw, "veiculos"), "veiculos")
sort(unique(tidied$cor_veiculo))
}
\seealso{
\code{\link[=clean_infosiga]{clean_infosiga()}} for the faithful processing this builds on, and
\link{infosiga_municipios} for the municipality lookup.
}
